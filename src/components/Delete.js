import './Table.component.scss';

const Delete = ({ Delete }) => {
    return (
        <div className="delete">
            <p className="p-delete">User has been deleted from the database.</p>
        </div>
    );
  }
   
  export default Delete;